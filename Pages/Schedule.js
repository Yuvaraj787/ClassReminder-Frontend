import { StyleSheet, View, Text, ScrollView, FlatList, TouchableOpacity, Alert, PermissionsAndroid, Pressable } from "react-native"

export default function Schedule() {
    return (
        <View>
            <Text>
                You can view complete schedule from me
            </Text>
        </View>
    )
}