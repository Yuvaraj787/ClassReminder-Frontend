import { StyleSheet, View, Text, ScrollView, FlatList, TouchableOpacity, Alert } from "react-native"

export default function Attendence() {
    return (
        <View>
            <Text>Im managing the Attendence</Text>
        </View>
    );
}